rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow read access to anyone for now
    match /{document=**} {
      allow read: if true;
    }

    // Allow writes only to authenticated users
    match /{document=**} {
      allow write: if request.auth != null;
    }
  }
}
